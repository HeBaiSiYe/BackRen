spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hb_siye?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: zcpsgsh233
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.aynu.ancientbuilding.domain.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 开启SQL日志
  global-config:
    db-config:
      id-type: auto  # 主键自增
      logic-delete-field: isDeleted  # 逻辑删除字段名
      logic-delete-value: 1  # 逻辑已删除值
      logic-not-delete-value: 0  # 逻辑未删除值

jwt:
  secret: "your-jwt-secret-key-here-at-least-256-bit-long"  # 建议至少32个字符
  expiration: 86400000  # 过期时间（毫秒），例如24小时

